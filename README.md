# Business & Travel Bulgaria 2026

Одностраничный статический лендинг в направлении «Черноморский модернистский атлас». Проект собран на React и Vite и подготовлен для размещения на GitHub Pages.

## Маршрут и структура

У сайта одна точка входа — `/`. Контент идёт в следующем порядке:

1. hero;
2. таймер до форума;
3. ключевые факты;
4. ценность и причины участвовать;
5. основной день и программа 12 сентября;
6. спикеры;
7. экскурсии и побережье;
8. форматы участия и стоимость;
9. отзывы;
10. FAQ;
11. контакт с организатором.

## Локальный запуск

```bash
npm install
npm run dev
```

Production-сборка:

```bash
npm run build
npm run preview
```

Vite использует относительный `base: './'`, поэтому страница и локальные изображения работают внутри подпапки репозитория GitHub Pages.

## Публикация на GitHub Pages

1. Создайте репозиторий на GitHub и загрузите проект в ветку `main`.
2. В настройках репозитория откройте **Settings → Pages**.
3. В поле **Source** выберите **GitHub Actions**.
4. Workflow `.github/workflows/pages.yml` соберёт каталог `dist` и опубликует сайт.

Для ручной проверки перед push выполните `npm run check`.

## Форматы участия

- **Только форум:** 12 сентября 2026 года, участие и кофе-брейк — **25 €**.
- **Полный пакет с отелем:** 8–13 сентября 2026 года — **500 €** для одного участника, **700 €** для двоих или **800 €** для троих. Перелёт и трансфер не входят в стоимость.

## Визуальная система

Основное направление — редакционный Atlas: холодная минеральная палитра, кобальтовые акценты, документальные фотографии, резкая сетка и контрастные типографические полосы. Локально подключены:

- `Sofia Sans Condensed Variable` — крупные заголовки;
- `Manrope Variable` — текст, навигация, метки и интерфейсные элементы.

## Заявки, реферальные ссылки и CRM

Все основные CTA ведут к организатору:

- Наталья Маслова;
- Telegram: `@maslovanataly` — `https://t.me/maslovanataly`.

В финальном contact-блоке подготовлен официальный Tally embed. Он появляется только при наличии валидного `VITE_TALLY_FORM_ID`; до этого контейнер остаётся пустым и скрытым, а Telegram продолжает работать как основной fallback.

Для локальной проверки создайте `.env.local`:

```env
VITE_TALLY_FORM_ID=ваш_id_формы
```

Для GitHub Pages добавьте ID в **Settings → Secrets and variables → Actions → Variables** с именем `TALLY_FORM_ID`. Workflow уже передаёт его в production-сборку.

Реферальная ссылка имеет вид `?ref=slug`. Сайт хранит проверенные first/last-touch значения 30 дней и передаёт их в скрытые поля Tally; имя, телефон и другие данные заявки в браузере не сохраняются. Сгенерировать все ссылки после публикации:

```bash
npm run referrals -- https://username.github.io/repository/
```

Полная схема формы и подключения к Airtable описана в `CRM_SETUP.md`.

## Изображения и права

Изображения хранятся локально в `public/images`; сайт не зависит от исходного Lovable-домена. Подтверждение прав на их публикацию остаётся обязательным launch gate: перед рекламным или публичным запуском необходимо подтвердить права либо заменить изображения лицензированными файлами под теми же именами.
