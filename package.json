{
  "name": "business-travel-bulgaria-2026",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "npx wrangler dev",
    "worker:deploy": "npx wrangler deploy",
    "check": "npm run test && vite build",
    "test": "node --test tests/*.test.js",
    "referrals": "node scripts/referral-links.mjs",
    "airtable:schema": "node scripts/airtable-schema.mjs"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "@fontsource-variable/manrope": "^5.3.0",
    "@fontsource-variable/sofia-sans-condensed": "^5.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.0",
    "vite": "^8.1.0"
  }
}
